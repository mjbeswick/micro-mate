{
  "name": "micro-mate-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -b --noEmit",
    "sync-assets": "node scripts/sync-assets.mjs",
    "gen-icons": "node scripts/gen-icons.mjs",
    "prebuild": "node scripts/sync-assets.mjs && node scripts/gen-icons.mjs"
  },
  "dependencies": {
    "chess.js": "^1.0.0-beta.8",
    "konva": "^9.3.16"
  },
  "devDependencies": {
    "sharp": "^0.34.5",
    "typescript": "^5.6.3",
    "vite": "^5.4.10",
    "vite-plugin-pwa": "^0.21.0",
    "vitest": "^2.1.4",
    "workbox-window": "^7.3.0"
  }
}
